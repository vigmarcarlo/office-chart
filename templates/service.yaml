apiVersion: v1
kind: Service
metadata:
  name: {{ include "office-helm.fullname" . }}
  labels:
    {{- include "office-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "office-helm.selectorLabels" . | nindent 4 }}
