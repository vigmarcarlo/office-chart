apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "office-helm.fullname" . }}-test-connection"
  labels:
    {{- include "office-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "office-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
